{
  "config": {
    "step": {
      "user": {
        "title": "Docker Manager",
        "description": "Choose the connection type to your Docker daemon.",
        "data": {
          "connection_type": "Connection type"
        }
      },
      "local": {
        "title": "Local connection",
        "description": "Connect via Unix socket (e.g. {example}).",
        "data": {
          "url": "Socket URL",
          "scan_interval": "Update interval (seconds)"
        }
      },
      "remote": {
        "title": "Remote connection",
        "description": "Connect via TCP (e.g. {example}).",
        "data": {
          "url": "TCP URL",
          "scan_interval": "Update interval (seconds)"
        }
      },
      "containers": {
        "title": "Container selection",
        "description": "{count} container(s) detected. Select the ones you want to monitor. Leaving all unchecked monitors everything.",
        "data": {
          "containers_include": "Containers to monitor"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to the Docker daemon. Check the URL and access rights."
    },
    "abort": {
      "already_configured": "Docker Manager is already configured for this instance."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Docker Manager Options",
        "description": "{count} container(s) available.",
        "data": {
          "containers_include": "Containers to monitor",
          "scan_interval": "Update interval (seconds)",
          "update_check_interval": "Auto update check interval (seconds)",
          "notify_on_down": "Notify when a container goes down",
          "enable_logs": "Enable container logs sensor",
          "logs_tail": "Number of log lines to keep (10-500)"
        }
      }
    }
  },
  "services": {
    "prune_images": {
      "name": "Prune Docker Images",
      "description": "Remove all unused Docker images to free up disk space.",
      "fields": {
        "entry_id": {
          "name": "Instance",
          "description": "Config entry ID (optional)."
        },
        "all_unused": {
          "name": "All unused images",
          "description": "Remove all images not used by running/stopped containers."
        }
      }
    },
    "check_all_updates": {
      "name": "Check All Updates",
      "description": "Check for image updates across all monitored containers.",
      "fields": {
        "entry_id": {
          "name": "Instance",
          "description": "Config entry ID (optional)."
        }
      }
    }
  }
}